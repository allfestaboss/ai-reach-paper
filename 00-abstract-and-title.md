# Abstract — 案B基準で書き直し（v2）

**Title**

> Who checks the answer key? Ground-truth defects in eight
> professional-practice benchmarks

代案: *A benchmark cannot validate its own ground truth: evidence from eight
professional-practice suites*（主張が明示的だが、やや説教くさい）

---

## 重心（v1 から移動）

> **ベンチマークの検査は、すべて参照解を基準に定義されている。
> だから参照解の誤りだけは、原理的に検出できない。**

v1 は3つの domain-dependence を重心に置いていた。関連研究を潰した結果、
**所見2 は HELM が、所見3 は τ-bench の `pass^k` が、所見1 は IR の augmentation 研究が
それぞれ枠組みを先に確立している**ことが分かった。現象の発見としては主張できない。

一方、**参照解の検証不能性**には競合が見つからなかった。そして 8業種・24課題・
欠陥15件という規模があるからこそ、これが逸話でなく所見になる。

---

## Abstract

Benchmarks are known to contain incorrect ground truth, and models are already
used to find it. Those results concern **sparse annotation errors** attached to
individual instances, located by statistical methods or by re-annotation. We
report a class they do not reach: **systematic errors in the program that
derives a reference solution**, which displace every case on the same code path
in the same direction. There is then no correct majority for statistical
detection to work against, and re-annotation reproduces the error whenever the
re-annotator follows the same specified procedure.

Such an error is also invisible to the benchmark's own machinery. A grader
compares a submission to the reference; calibration recomputes the reference
along its specified procedure; adversarial cases are generated by corrupting the
reference. All three are functions of the reference solution, and **none can
detect an error in it.**

We report evidence from eight benchmarks — 24 tasks across civil and
architectural CAD, quantity surveying, geotechnical assessment, mechanical
tolerancing, building information modelling, customs classification and tax law
— built to a single design in which the inputs, the procedure and the reference
solutions are all drawn from national standards and administrative rulings
rather than authored by us. Fifteen defects were found on the examination side
rather than in the answers. **Fourteen of the fifteen were located by the
solvers**, which hold the raw inputs but not the answer key; our own calibration
and adversarial suites found one. In the sharpest case, a reference solution
integrated a geotechnical index below the bottom of the borehole, so that
60 percent of one reported value came from depth that was never drilled. It
passed the grader, passed calibration against hand computation — which repeated
the same integration bounds — and passed a twelve-case adversarial suite, every
member of which was generated from the reference and therefore inherited them.

The same design lets us measure how far three known effects vary across
domains, holding the procedure fixed. Supplying the governing material moves a
drawing-generation task from 25 to 97 out of 100, while in customs
classification enlarging it from 0.81 to 4.4 million characters leaves all 61
answers unchanged. Scores saturate while cost does not: in one domain
configurations tie at 100/100 across two task sizes while the cost gap widens
from 1.37 to 2.6 times. Repeated under identical conditions,
the fraction of cases whose answer changes every time ranges from 0 to 82
percent — and in half of the unstable cases the number of correct answers is
identical across runs while the set of answers is not, a pattern that
success-rate reliability metrics cannot express.

All eight benchmarks, their reference solutions, their defect records and their
run logs are released under permissive terms with persistent identifiers.

*(≈ 443 words。**arXiv の上限は1,920文字＝約300語なので超過**。下の投稿版を使う)*

---

## Abstract — 投稿版（arXiv の1,920文字以内）

Benchmarks are known to contain incorrect ground truth, and models are used to
find it. That work concerns sparse annotation errors on individual instances.
We report a class it does not reach: systematic errors in the program deriving a
reference solution, which displace every case on the same code path identically.
No correct majority remains for statistical detection, and re-annotation
reproduces the error whenever it follows the same procedure. It is equally
invisible to the benchmark's own machinery: the grader compares against the
reference, calibration recomputes it along that procedure, and adversarial cases
are generated from it. None can detect an error in it.

Evidence comes from eight benchmarks — 24 tasks spanning CAD, quantity
surveying, geotechnics, tolerancing, BIM, customs classification and tax law —
built to one design in which inputs, procedure and reference solutions all come
from national standards and administrative rulings. Fifteen defects
were found on the examination side rather than in the answers, and fourteen by
the solvers, which hold the raw inputs but not the answer key. In the sharpest
case a reference integrated a geotechnical index below the bottom of the
borehole, placing 60 percent of a reported value in undrilled depth; it passed
the grader, calibration to a difference of zero, and all twelve adversarial
cases.

The same design measures how far three documented effects vary by domain.
Supplying the governing material ranges from moving a task 25 to 97 out of 100
to changing none of 61. Where scores tie, cost does not: six runs of one task all
scored 100/100 while the two configurations differed 2.11x in tokens, a gap 15.6
pooled standard deviations wide. Answers change under identical repetition in 0 to 82
percent of cases, half of them scoring identically by different routes. All
eight benchmarks, their defect records and run logs are released with persistent
identifiers.

## v1 から何が変わったか

| | v1（案A） | v2（案B） |
|---|---|---|
| 重心 | 3つの domain-dependence | **参照解の検証不能性** |
| 3所見の扱い | 主要な貢献 | **「既知の効果の業種差の測定」に格下げ** |
| 欠陥15件 | §4.4 に埋もれていた | **主要な貢献に昇格** |
| 争われやすさ | 高い（τ-bench / HELM と衝突） | 低い（競合が見つからない） |
| 8業種の意味 | 横断のため | **欠陥15件を出す母数として。むしろ強い** |

**「既知の効果」と自分から書いたのが v2 の要点。** 隠して刺されるより、
先に位置づけて「では何が新しいのか」を自分で答えるほうが強い。

---

## 本文への波及（3・4章の修正）

1. **3.2 / 3.3 の主張文を書き換える。**
   「発見した」→「**既知の現象の、業種間での大きさを測った**」。
   - 3.2 に HELM を引く。efficiency を accuracy と並置する枠組みは既にある
   - 3.3 に τ-bench を引く。ただし **`pass^k` は成否の二値なので、
     「点数は動かず中身が入れ替わる」を表現できない**と明記する（ここは本当に残る）
   - 3.1 に IR の augmentation 研究を引く。mixed results は既知

2. **4.1.1 と 4.4 を3章へ昇格させる。**
   いまは Threats の中にある。主要な貢献なので Findings 側に移す。
   Threats には「この構造的欠陥は本研究自身にも当てはまる」という形で残す。

3. **1章の分類を2型→4型にする。**
   試験問題型 / 単一ドメイン型 / **信頼性型（τ-bench, ReliabilityBench）** /
   **多指標型（HELM）**。ここを2型のままにすると確実に刺される。
